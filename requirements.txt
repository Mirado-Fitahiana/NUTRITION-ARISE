# ARISE Nutrition Service — dépendances directes
# Fichier en UTF-8 (l'ancienne version était en UTF-16LE, `pip install -r` échouait).
# Les dépendances transitives sont résolues par pip ; ne pas y ajouter de paquet
# qui n'est pas importé directement par le code.

# --- Web ---
fastapi==0.140.7
uvicorn[standard]==0.51.0
python-multipart==0.0.32
Jinja2==3.1.6

# --- Configuration et validation ---
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic-extra-types==2.11.1
email-validator==2.3.0
python-dotenv==1.2.2

# --- Base de données ---
SQLAlchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
greenlet==3.5.4

# --- Authentification (lot 1 — vérification JWT RS256 via JWKS) ---
PyJWT[crypto]==2.10.1

# --- Clients externes ---
httpx==0.28.1

# --- Collecte (lot 4, conditionnel) — analyse HTML en pur Python, rien à compiler sous 3.14 ---
beautifulsoup4==4.14.2

# --- Seed et observabilité ---
PyYAML==6.0.3
sentry-sdk==2.66.1
